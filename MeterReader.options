# We can only need 6 digital sensors
MeterReader.Settings.risingEdgeAmounts   max_count:6
MeterReader.Settings.fallingEdgeAmounts  max_count:6

# Define a maximum length
MeterReader.LogMessage.text              max_size:40